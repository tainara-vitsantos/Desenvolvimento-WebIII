@model IEnumerable<Academico.Models.Matricula>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataMatricula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Curso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aluno)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataMatricula)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Curso.CursoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aluno.AlunoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MatriculaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MatriculaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MatriculaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
